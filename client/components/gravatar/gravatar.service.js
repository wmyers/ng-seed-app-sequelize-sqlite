'use strict';

angular.module('gravatarMdul', []).
factory('gravatarSrvc', [
                          '$http',
                          '$q',
                          '$window',
                          function(
                            $http,
                            $q,
                            $window
                          ) {

      function getImage(email, size, rating, def){
        //http://www.gravatar.com/avatar/HASH

      }

}]);
